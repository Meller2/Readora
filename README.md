# Readora

Premium Flutter Android reader prototype with an Apple-like liquid glass UI,
translucent bottom navigation, large rounded book cards, and smooth screen
transitions.

## Run

Flutter SDK is not currently available in this environment. After installing
Flutter or adding it to `PATH`, run:

```powershell
flutter pub get
flutter run
```
